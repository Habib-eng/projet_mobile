import { IconButton } from "react-native-paper";

export default function Action({ icon, color, target, action }) {
  return (
    <>
      {/* <IconButton onPress={() => action} icon={icon} size={30} color={color} mode="contained" /> */}
    </>
  );
}
